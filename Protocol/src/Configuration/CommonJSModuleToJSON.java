package Configuration;

/**
 * Convierte un CommonJS Module tipo "export default function () { return { ... } }" a un objeto JSON
 * Se usará para convertir el archivo state.js del store en Vuex de la appWeb correspondiente a los componentes de eolian fénix/áuriga
 * A un archivo JSON que se pueda pasar a la clase JSONToComponent para automatizar el protocolo
 * Es decir, state.js será el archivo de configuración de los Componentes.
 */
public class CommonJSModuleToJSON {
}
